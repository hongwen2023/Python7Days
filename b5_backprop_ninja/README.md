# B5 — makemore Part 4：Becoming a Backprop Ninja 🔒 ⭐ 试金石

**视频**：Building makemore Part 4: Becoming a Backprop Ninja

## 将要学会

不用 `loss.backward()`，手工反推整个两层网络 + BatchNorm + cross entropy 的每一层梯度，
和 PyTorch autograd 逐张量对账。全系列最硬的一集，通过 = 真正理解反向传播。

## 前置

- 18.06 第 14-16 课（投影/最小二乘）此时应已完成
- 矩阵求导会大量出现，卡住时用精算里熟悉的标量链式法则先退化理解

## 解锁条件

B4 测试全绿并通过 Claude review。
