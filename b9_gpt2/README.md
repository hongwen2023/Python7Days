# B9 — Let's reproduce GPT-2 (124M) 🔒

**视频**：Let's reproduce GPT-2 (124M)

## 将要学会

把 B7 的玩具 GPT 升级为真实规模的训练工程：
- 混合精度、torch.compile、FlashAttention
- 学习率调度、梯度累积、分布式数据并行（DDP）
- 加载并对齐 OpenAI 的 GPT-2 权重验证正确性

## 毕业后

进入 C 线毕业项目 **nanochat**：$100 预算从零训练完整的 ChatGPT 克隆
（分词 → 预训练 → SFT → RL → 推理 → Web UI）。

## 解锁条件

B8 测试全绿并通过 Claude review。
